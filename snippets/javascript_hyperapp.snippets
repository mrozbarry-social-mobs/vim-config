snippet @hyperapp "Full hyperapp boilerplate"
import { app, h, text } from 'hyperapp';

const initialState = {
};

app({
	init: initialState,

	view: (state) => h('div', {}, text('Hello from Hyperapp')),

	subscriptions: (state) => [],

	node: document.querySelector('#app'),
});
endsnippet

snippet @haEffect "Hyperapp effect builder"
const ${1:effectName}Fx = (dispatch, props) => {
};
export const $1 = (props) => [$1Fx, props];
endsnippet
